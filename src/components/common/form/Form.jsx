import React, { useState } from 'react'

export default function Form({getData, defaultValues}) {
    const [data, setData] = useState(defaultValues);

    

    return (
        <form action="" >

        </form>
    )
}
